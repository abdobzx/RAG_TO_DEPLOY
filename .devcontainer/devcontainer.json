{
  "name": "Python 3",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "RAG.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "[ -f /workspaces/RAG_TO_DEPLOY/packages.txt ] && sudo apt update && sudo apt upgrade -y && sudo xargs apt install -y </workspaces/RAG_TO_DEPLOY/packages.txt; [ -f /workspaces/RAG_TO_DEPLOY/requirements.txt ] && pip3 install -r /workspaces/RAG_TO_DEPLOY/requirements.txt; pip3 install streamlit; echo '✅ Packages installed and Requirements met'",
  "postAttachCommand": {
    "server": "streamlit run RAG.py --server.enableCORS false --server.enableXsrfProtection false"
  },
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}
